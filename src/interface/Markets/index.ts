export type MetadataResponse = [string, string];

export type PythPriceId = {
  symbol: string;
  feedId: string;
};

export type MarketMetadata = {
  marketName: string;
  symbol: string;
  feedId: string;
};
